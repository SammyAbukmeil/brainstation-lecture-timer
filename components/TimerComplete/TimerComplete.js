import styles from "./TimerComplete.module.scss";

const TimerComplete = () => {
  return (
    <article className={styles.complete}>
      <p>Update the schedule</p>
    </article>
  );
};

export default TimerComplete;
